# Crea la variable message que almacene la concatenacion
# de notification seguida de kind usando el operador +.

notification = "New appointment: "
kind = "meeting"
message = notification + kind
print(message)
