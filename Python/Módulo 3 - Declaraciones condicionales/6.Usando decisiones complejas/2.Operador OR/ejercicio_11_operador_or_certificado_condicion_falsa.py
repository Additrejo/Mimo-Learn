# Con or, el código se omite solo si todas las condiciones son False,
# como average_grade == "A" y final_score >= 1500.

average_grade = "B"
final_score = 1400

if average_grade == "A" or final_score >= 1500:
    print("Certificate achieved!")

# En este caso ninguna condición se cumple, por lo que no hay salida en consola.
