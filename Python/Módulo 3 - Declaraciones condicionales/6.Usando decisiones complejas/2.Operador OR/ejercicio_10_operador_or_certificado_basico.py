# Para ejecutar código cuando una de las condiciones es True, usamos el operador or.

average_grade = "A"
final_score = 1400

if average_grade == "A" or final_score >= 1300:
    print("Certificate achieved!")

# Salida esperada:
# Certificate achieved!
